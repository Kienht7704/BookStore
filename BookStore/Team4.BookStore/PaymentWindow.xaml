<Window x:Class="Team4.BookStore.PaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Payment" Height="620" Width="500" 
        Background="#87cefa" WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True"
        Loaded="Window_Loaded">

    <Border CornerRadius="15" Background="White" BorderBrush="#87cefa" BorderThickness="3">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="💳 Payment" FontSize="22" FontWeight="Bold" Foreground="#2d3748" HorizontalAlignment="Center"/>
                <TextBlock Text="Select payment method" FontSize="12" Foreground="#87cefa" HorizontalAlignment="Center" Margin="0,3,0,0"/>
            </StackPanel>

            <!-- Total Amount -->
            <Border Grid.Row="1" Background="#f0ffff" CornerRadius="8" Padding="10" Margin="0,0,0,10" BorderBrush="#87cefa" BorderThickness="2">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Total Amount" FontSize="12" Foreground="#2d3748" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="TotalAmountLabel" Text="$0.00" FontSize="32" FontWeight="Bold" Foreground="#10b981" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Payment Methods -->
            <StackPanel Grid.Row="2" Margin="0,0,0,10">
                <RadioButton x:Name="CashRadio" GroupName="PaymentMethod" IsChecked="True" Margin="0,0,0,10">
                    <RadioButton.Template>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="border" Background="#f0ffff" CornerRadius="8" Padding="12" Cursor="Hand"
                                    BorderBrush="#87cefa" BorderThickness="2">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💵" FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="Cash Payment" FontSize="14" FontWeight="SemiBold" Foreground="#2d3748"/>
                                        <TextBlock Text="Pay with cash" FontSize="10" Foreground="#87cefa"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#10b981"/>
                                    <Setter TargetName="border" Property="Background" Value="#f0ffff"/>
                                    <Setter TargetName="border" Property="BorderThickness" Value="3"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#e0f7ff"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </RadioButton.Template>
                </RadioButton>

                <RadioButton x:Name="QRRadio" GroupName="PaymentMethod">
                    <RadioButton.Template>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="border" Background="#f0ffff" CornerRadius="8" Padding="12" Cursor="Hand"
                                    BorderBrush="#87cefa" BorderThickness="2">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📱" FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="QR Code Payment" FontSize="14" FontWeight="SemiBold" Foreground="#2d3748"/>
                                        <TextBlock Text="Scan QR to pay" FontSize="10" Foreground="#87cefa"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#10b981"/>
                                    <Setter TargetName="border" Property="Background" Value="#f0ffff"/>
                                    <Setter TargetName="border" Property="BorderThickness" Value="3"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#e0f7ff"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </RadioButton.Template>
                </RadioButton>
            </StackPanel>

            <!-- QR Code Display (shown when QR selected) -->
            <Border x:Name="QRCodePanel" Grid.Row="3" Visibility="Collapsed" 
                    Background="#f0ffff" CornerRadius="8" Padding="10" Margin="0,0,0,5" BorderBrush="#87cefa" BorderThickness="2">
                <StackPanel HorizontalAlignment="Center">
                    <Border Width="180" Height="180" Background="White" CornerRadius="8" Margin="0,0,0,8" Padding="3" BorderBrush="#87cefa" BorderThickness="2">
                        <Image x:Name="QRCodeImage" Stretch="Uniform"/>
                    </Border>
                    <TextBlock Text="Scan QR code to pay" FontSize="12" Foreground="#10b981" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <!-- Buttons -->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="CancelButton" Grid.Column="0" Content="Cancel" 
                        Background="#ef4444" Foreground="White" FontSize="16" FontWeight="Bold"
                        Height="50" Margin="0,0,5,0" Cursor="Hand" Click="CancelButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button x:Name="ConfirmButton" Grid.Column="1" Content="✓ Confirm Payment" 
                        Background="#10b981" Foreground="White" FontSize="16" FontWeight="Bold"
                        Height="50" Margin="5,0,0,0" Cursor="Hand" Click="ConfirmButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>